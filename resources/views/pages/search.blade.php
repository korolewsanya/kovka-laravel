{{-- Берём общую рамку сайта: шапка, подвал, стили — всё оттуда --}}
@extends('layouts.app')

{{-- Заголовок вкладки браузера.
     request('q') — берём то, что пользователь ввёл в поиск, из адреса (?q=...).
     Склеиваем со строкой, чтобы получилось: "Результаты поиска: мангал" --}}
@section('title', 'Результаты поиска: ' . request('q'))

{{-- Содержимое страницы: вставится в @yield('content') главного шаблона --}}
@section('content')
{{-- Ограничиваем ширину страницы и центрируем её --}}
<div class="max-w-6xl mx-auto">

{{-- Шапка страницы: кнопка "На главную" и заголовок --}}
    <div class="flex items-center gap-4 mb-6">
        <a href="{{ route('home') }}" class="btn btn-ghost btn-sm">
            ← На главную
        </a>
        <h1 class="text-3xl font-bold">
            {{-- Показываем тот же запрос, что ввёл пользователь, синим цветом --}}
            Результаты поиска: <span class="text-primary">"{{ request('q') }}"</span>
        </h1>
    </div>

    {{-- Подключаем Livewire-компонент, который рисует сетку найденных товаров.
         Двоеточие перед query означает "это PHP-значение",
         т.е. в компонент передаётся результат request('q') — то, что ввёл пользователь. --}}
    <livewire:search-results :query="request('q')" />
</div>
@endsection
